Vendored from Racer & Pacer (frontend/src) so the portfolio renders the real
components rather than lookalikes. Copied, not linked: this repo has no
dependency on that one, and a portfolio should keep working if that codebase
moves on.

They will drift. When they do, the honest fix is to re-copy and note the date,
not to quietly edit these to look better than what shipped.

Copied 2026-08-14 from racerandpacer @ frontend/src:
  theme.ts, components/RPBadge.tsx, RPButton.tsx, RPIconBox.tsx, RPPill.tsx
