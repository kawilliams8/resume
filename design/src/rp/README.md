Vendored from Racer & Pacer (frontend/src) so the portfolio renders the real
components rather than lookalikes. Copied, not linked: this repo has no
dependency on that one.

They will drift. When they do, re-copy and update the date here — do not edit
these to look better than what shipped.

Copied 2026-08-14 from racerandpacer @ frontend/src:
  theme.ts, components/RPButton.tsx
(RPBadge, RPIconBox, RPPill get re-copied the same way when a demo needs them.)
Re-copied 2026-08-15: RPBadge.tsx, RPPill.tsx (badge vocabulary and drift demos).
